const authController = require("./authController");
const categoryController = require("./categoryController");
const notFound = require("./notFound");

module.exports = { authController, categoryController, notFound };
